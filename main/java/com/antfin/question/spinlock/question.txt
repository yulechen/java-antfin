题目二：
   java互斥锁和自旋锁有什么差别，要求提一个自旋锁的实现（用最简单的ticket模式就好了）
   要求：
     1.满足自旋锁的基本特性。
     2.要求提供测试代码。

--
  互斥锁：
     1、互斥锁，一般情况下，会休眠没有获取到锁的线程，中间会有线程上下文切换。让出cpu 控制权给其他线程。

  自旋锁：
     1、程序一直循环，知道获取到锁。如果在较短时间内，没有获取到锁，cpu 使用会很高。会一直占用cpu 资源。
     2、开销较互斥锁交少，实现复杂度底。